.search-results {
	&__head {
		background: $borderGray;
		margin-top: -23px;
		font-size: 18px;
		line-height: 26px;
		padding: 22px 0 19px 8px;
		letter-spacing: 1.8px;
		&-name {
			color: $blue;
			text-transform: uppercase;
			letter-spacing: 5px;
		}
	}
}